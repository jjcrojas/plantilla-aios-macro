interface:
  display_name: "Generar trimestral AIOS"
  short_description: "Consolida cortes trimestrales en un libro"
  default_prompt: "Usa $generar-trimestral-aios para generar en un único libro los cortes trimestrales del período o rango que te indique, aplicando la contingencia de Excel cuando corresponda."
